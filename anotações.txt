HTML>
    - Semântica, significado

CSS>
    - Estilo, forma

### Dicas
    -> O tamanho de imagens é muito importante no site, pois o usuário que irá acessar precisará baixar todas as imagens, imagens pesadas resultam em uma maior demora para o site ser carregado.


### Atalhos
    Inserir ! em um documento vazio sugere a estrutura padrão do html
    CTRL + SHIFT + P        -> Abre menu de configurações/atalhos do visual code # envelope de assinatura

### Tags padrão
    # <!DOCTYPE html>           -> Indica ao navegador a versão de html utilizada no código, que se não especificada é o html5.

### Tags HEAD (Configurações do site)
    # <meta charset="">         -> Define a codificação utilizada pelo site, utilizar UTF-8, para funcionar acentuação
    # <meta viewport"...>       -> Informa ao navegador que o body irá utilizar 100% do espaço disponivel do navegador
    # <title>

### Tags BODY (Conteúdo do site)
    <h1>
    <h3>
    <p>
    <br>
    <img src="">
    <address>

### Códigos de caracteres
    # <         -> &lt; (less than)
    # <         -> &gt; (greater than)
    # ®️         -> &reg; (Marca registrada)
    # ©️         -> &copy;
    # ™️         -> &trade; 
    # €         -> &euro;
    # £         -> &pound;
    # ¥         -> &yen;
    # ¢         -> &cent;
    # δ         -> &delta;
    # Δ         -> &Delta;
    # EMOJIS    -> &#x >> 1F596 <<;